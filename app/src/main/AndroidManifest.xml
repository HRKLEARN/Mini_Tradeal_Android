<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tradeal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_tradeal_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mini_Android_tradeal">
        <activity
            android:name=".ProductDetailsActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".ui.activities.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.navigationActivity" />
        <activity android:name=".ui.activities.SignInActivity" />
        <activity android:name=".ui.activities.SignupActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
    </application>

</manifest>